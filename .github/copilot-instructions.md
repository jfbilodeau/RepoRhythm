- Avoid nullable types.
- Ensured all class variable that are nuallable are initialized to defaults or string.Empty.

- All classes should begin with the prefix `Jf`
- All properties should always use classical Latin words instead of English.